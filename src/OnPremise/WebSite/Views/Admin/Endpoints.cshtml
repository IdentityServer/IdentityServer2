@model Thinktecture.IdentityServer.Web.ViewModels.Administration.EndpointConfigurationModel

@{
    ViewBag.Title = "Endpoint Configuration";
}

<h2>
    Endpoint Configuration
    <a href="http://wiki.thinktecture.com/IdentityServer.EndpointsConfiguration.ashx" target="_blank">(help)</a>
</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm())
{
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Configuration</legend>

        @Html.EditorForModel()

        @if (ViewBag.SaveEnabled)
        { 
            <p>
                <input type="submit" value="Save" />
            </p>
        }
    </fieldset>
}

<div>
    @Html.ActionLink("Back", "Index")
</div>

