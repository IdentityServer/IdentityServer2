@model List<string>

@{
    ViewBag.Title = "Client Certificates";
}

<h2>Client Certificates</h2>

<p>
    @Html.ActionLink("Add New", "add")
</p>

<ul>
    @foreach (var user in Model)
    { 
        <li>
            @user
            @Html.ActionLink("(edit)", "edit", new { username = user })
            @Html.ActionLink("(delete)", "deleteall", new { username = user })
        </li>
    }
</ul>

<p>
    @Html.ActionLink("Back", "index", "admin")
</p>


