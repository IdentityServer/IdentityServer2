@model IdentityProvidersViewModel

@{
    ViewBag.Title = "Identity Providers";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary("Error deleting identity providers")

    <fieldset class="editor">
        <legend>@ViewBag.Title</legend>
        @{
            var list = Model.IdentityProviders;
        }
        @for (int i = 0; i < list.Length; i++)
        {
            <div>
                @Html.HiddenFor(x=>list[i].Name)
                @Html.LabelFor(x => list[i].Delete, list[i].DisplayName)
                @Html.CheckBoxFor(x => list[i].Delete)
            </div>
        }

        <div class="buttons">
            <button type="submit" name="action" value="delete">Delete Selected</button>
            <button type="submit" name="action" value="new">New</button>
            <button type="reset">Cancel</button>
        </div>

    </fieldset>
}

